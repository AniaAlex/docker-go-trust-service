server:
  host: "0.0.0.0"
  port: "6001"
  frequency: "5m"

logging:
  level: "info"
  format: "text"
  output: "stdout"

pipeline:
  timeout: "30s"
  max_request_size: 10485760
  max_redirects: 3
  

security:
  rate_limit_rps: 100
  enable_cors: true
